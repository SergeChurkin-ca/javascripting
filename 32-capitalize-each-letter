// my name is jon => My Name Is Jon

function splitAndMerge(string, separator) {
    return string.split(' ')
        .map((x) => x[0].toUpperCase()
            .concat(x.slice(1))
        ).join(' ')

}
console.log(splitAndMerge("my name is jon"))
