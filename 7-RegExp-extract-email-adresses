// extract email adresses from given string usegin regex
let myStr =
    "Hello world javascript 123 this works gibrish sample@email.com I love that, contact me example@example.com";

let newStr = /[a-z]+[@]+[a-z]+[.]\w+/g;

let emailData = myStr.match(newStr);
console.log(emailData);
